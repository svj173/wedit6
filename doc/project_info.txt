Инфа о программе


WEdit6 - есть ошибки. разобраться.
- В Идеи не все библиотеки привязаны, но АНТ собирает без ошибок.

1) Деплой

запускать в директории ant - т.к. все внутренние привязки к путям-файлам идут от нее!

ant -buildfile wedit6.xml deploy.linux.common

ant -Djava.home=/usr/lib/jvm/java-8-openjdk-amd64 -buildfile wedit6.xml deploy.linux.locale
