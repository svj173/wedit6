### Главный Конфиг редактора

# Титл. Надпись на фрейме Содержимого, перед датой сборки и полной версией редактора
wedit.title=Мой редактор

# путь до директории, где лежат функции (допускаются поддиректории)
functions.dir=conf/function

# путь до директории, где лежат дефолтные структуры книг - наборы. (допускаются поддиректории)
elements.dir=conf/element
